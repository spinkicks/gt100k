/**
 * `@gt100k/ui` — the shared product shell. Two things only: the surfaces registry (what surfaces
 * exist, who they are for, and where they live in THIS environment) and the ProductHeader that
 * carries the product identity across every adult surface.
 */
export * from "./surfaces.js";
export * from "./product-header.js";
