// Recorded TFY judge response (a .ts file — NOT .json; the repo tsconfig does not set
// resolveJsonModule, so a JSON import would fail tsc -b). Synthetic, no PII.
export const JUDGE_RESPONSE = {
  coverage: 0.72,
  rationale: "Specific mechanism described with a personal reason.",
  thin: false,
};
